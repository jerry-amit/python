# Replace the double space from problem 3 with single spaces

name = "jerry   is   a   good   boy"

print(name.replace("   "," "))

print(name)# string is immutable which means you cannot change them by running functions on them